# Story 1.1: Initialisation du projet

## Status
Done

## Story
**As a** developer,
**I want** a properly configured project with React frontend and Fastify backend,
**so that** I can start development quickly with a simple structure.

## Acceptance Criteria
1. Projet initialisé avec pnpm et un seul `package.json`
2. Structure `src/client/` créée avec Vite + React 18 + TypeScript
3. Structure `src/server/` créée avec Fastify + TypeScript
4. Dossier `src/shared/` créé pour types partagés
5. Scripts `dev`, `build`, `start`, `lint` fonctionnels
6. `.gitignore`, `.env.example` et `README.md` configurés

## Tasks / Subtasks
- [x] Initialiser le projet (AC: 1)
  - [x] Créer `package.json` avec toutes les dépendances
  - [x] Configurer `tsconfig.json` (paths aliases)
  - [x] Créer `vite.config.ts` avec proxy API
- [x] Créer structure frontend `src/client/` (AC: 2)
  - [x] Créer `main.tsx` et `App.tsx`
  - [x] Créer `index.html` dans src/client
  - [x] Configurer TailwindCSS
- [x] Créer structure backend `src/server/` (AC: 3)
  - [x] Créer `index.ts` (entry point)
  - [x] Créer `app.ts` (Fastify setup)
  - [x] Configurer @fastify/static pour prod
- [x] Créer types partagés `src/shared/` (AC: 4)
  - [x] Créer `types.ts` avec interfaces de base
- [x] Configurer scripts npm (AC: 5)
  - [x] Script `dev` : Vite + tsx watch (concurrently)
  - [x] Script `build` : Build client + server
  - [x] Script `start` : Fastify sert tout sur :3000
  - [x] Script `lint` : ESLint
- [x] Finaliser configuration (AC: 6)
  - [x] Créer `.gitignore`
  - [x] Créer `.env.example`
  - [x] Créer `README.md` avec instructions

## Dev Notes

### Architecture References
- Architecture v3.0 : Structure simplifiée, port unique :3000
- Tech stack : Vite, React 18, Fastify, TypeScript, TailwindCSS

### Structure cible
```
looptrading/
├── src/
│   ├── client/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   └── index.css
│   ├── server/
│   │   ├── index.ts
│   │   └── app.ts
│   └── shared/
│       └── types.ts
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── .env.example
└── .gitignore
```

### Dépendances principales
```json
{
  "dependencies": {
    "react": "^18.x",
    "react-dom": "^18.x",
    "react-router-dom": "^6.x",
    "@tanstack/react-query": "^5.x",
    "fastify": "^4.x",
    "@fastify/static": "^7.x"
  },
  "devDependencies": {
    "vite": "^5.x",
    "@vitejs/plugin-react": "^4.x",
    "typescript": "^5.x",
    "tailwindcss": "^3.x",
    "tsx": "^4.x",
    "concurrently": "^8.x"
  }
}
```

### Testing
- Vitest configuré mais tests écrits dans stories ultérieures

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-25 | 0.1 | Création initiale de la story | Sarah (PO) |
| 2026-01-25 | 0.2 | Mise à jour pour architecture v3.0 simplifiée | Winston (Architect) |
