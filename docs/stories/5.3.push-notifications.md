# Story 5.3: Notifications push navigateur

## Status
Draft

## Story
**As a** user,
**I want** browser push notifications,
**so that** I get instant alerts when the app is open.

## Acceptance Criteria
1. Service Worker configuré pour push notifications
2. Permission demandée au premier lancement
3. Notification affiche : symbol, stratégie, score
4. Clic sur notification → page détail de l'action
5. Option enable/disable dans settings

## Tasks / Subtasks
- [ ] Configurer Service Worker (AC: 1)
  - [ ] Créer `sw.js` pour notifications
  - [ ] Enregistrer SW au chargement app
  - [ ] Gestion événement `push`
  - [ ] Gestion événement `notificationclick`
- [ ] Implémenter demande de permission (AC: 2)
  - [ ] Modal explicatif au premier lancement
  - [ ] Appel `Notification.requestPermission()`
  - [ ] Stocker état permission
  - [ ] Ne pas re-demander si refusé
- [ ] Créer contenu notification (AC: 3)
  - [ ] Titre : "🚨 Alerte: SYMBOL"
  - [ ] Body : stratégie + score
  - [ ] Icon : logo app
  - [ ] Badge : indicateur visuel
- [ ] Implémenter navigation au clic (AC: 4)
  - [ ] Event `notificationclick`
  - [ ] Ouvrir/focus fenêtre app
  - [ ] Naviguer vers `/stocks/:symbol`
  - [ ] Fermer notification
- [ ] Ajouter toggle dans settings (AC: 5)
  - [ ] Champ `pushNotifications` dans UserSettings
  - [ ] Toggle UI dans page Settings
  - [ ] Gérer révocation permission navigateur

## Dev Notes
_À compléter après création du document d'architecture_

### Architecture References
- FR4 : Notifications push

### Service Worker Push Handler
```javascript
self.addEventListener('push', event => {
  const data = event.data.json();
  event.waitUntil(
    self.registration.showNotification(data.title, {
      body: data.body,
      icon: '/icon-192.png',
      badge: '/badge-72.png',
      data: { url: data.url }
    })
  );
});

self.addEventListener('notificationclick', event => {
  event.notification.close();
  event.waitUntil(
    clients.openWindow(event.notification.data.url)
  );
});
```

### Polling Integration
- Frontend poll `/api/alerts` toutes les 30s via TanStack Query
- Détection de nouvelles alertes par comparaison avec état précédent
- Affichage notification locale quand nouvelle alerte détectée

### Dependencies
- Story 5.1 (Alert engine) doit être complétée
- Story 1.4 (Frontend shell) doit être complétée

### Testing
- Tests unitaires avec Vitest

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-25 | 0.1 | Création initiale de la story | Sarah (PO) |
| 2026-01-25 | 0.2 | Remplacement WebSocket par polling (arch v3.0) | Winston (Architect) |
